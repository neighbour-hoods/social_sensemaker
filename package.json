{
  "name": "rep_interchange",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "build": "CARGO_TARGET_DIR=target cargo build --release --target wasm32-unknown-unknown && scripts/package-dnas.sh",
    "clippy": "CARGO_TARGET_DIR=target cargo clippy --target wasm32-unknown-unknown",
    "doc": "CARGO_TARGET_DIR=target cargo doc --target wasm32-unknown-unknown --open",
    "test": "cd test && npm test"
  },
  "author": "",
  "license": "ISC"
}
